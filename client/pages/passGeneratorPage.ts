export function renderPassGeneratorPage(container: HTMLElement): void {
  container.innerHTML = `
      <h2>Workout Generator</h2>
      <div id="generator">Here you can create a new workout session in just a few seconds...</div>
    `;
}
